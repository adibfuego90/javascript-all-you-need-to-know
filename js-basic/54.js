// Setting object properties in js

let point = {
  x: 10,
  y: 20,
  z: 30,
};

// Dot notation :
point.x = 1000;
console.log(point);

// Array notation :
let prop = "z";
point[prop] = 3000;
console.log(point);
